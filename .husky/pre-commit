if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is not installed; install it (e.g. 'brew install gitleaks') to enable the pre-commit secret scan." >&2
  exit 1
fi
gitleaks git --staged --redact --config .gitleaks.toml
